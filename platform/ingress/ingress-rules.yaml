apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rideshare-api-gateway
  namespace: rideshare
  annotations:
    nginx.org/ssl-redirect: "true"
    nginx.org/client-max-body-size: "10m"
    nginx.org/proxy-read-timeout: "60s"
    nginx.org/proxy-send-timeout: "60s"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - rideshare.teleioslabs.net
      secretName: rideshare-api-tls
  rules:
    - host: rideshare.teleioslabs.net
      http:
        paths:
          # User service - authentication, profiles
          - path: /api/v1/rider
            pathType: Prefix
            backend:
              service:
                name: rider-service
                port:
                  number: 80

          # Ride service - ride requests, matching, status
          - path: /api/v1/trips
            pathType: Prefix
            backend:
              service:
                name: trip-service
                port:
                  number: 80

          # Driver service - driver management, availability
          - path: /api/v1/drivers
            pathType: Prefix
            backend:
              service:
                name: driver-service
                port:
                  number: 80

          # Matching service - real-time tracking
          - path: /api/v1/matching
            pathType: Prefix
            backend:
              service:
                name: matching-service
                port:
                  number: 80

          # Notification service - push, SMS, email
          - path: /api/v1/email
            pathType: Prefix
            backend:
              service:
                name: email-service
                port:
                  number: 80

          # Frontend service - static content
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-service
                port:
                  number: 80
